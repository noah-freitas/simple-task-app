# Very Simple Tasks Example App.

Run the app directly from the console: paste the app.js source code into the browser console.  Add tasks, refresh the page and repaste to see simple local storage persistance.

